<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

      <a href = "<%= root_url %>"><%= image_tag("banner.jpg", :alt => "borrow a bash logo", :class => "navbar-brand")%>
	</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <form class="navbar-form navbar-left" action = "/categories">
      <div class = "search">
        <div class="form-group">
          <input type="text" class="form-control js-search-term" name = "search_word" placeholder="What are you looking for?" value = "<%= @search_term if @search_term%>">
        </div>
        <button class="btn btn-default btn-blue">Search</button>
        </div>
      </form>
      <ul class="nav navbar-nav navbar-right">

      		<%if user_signed_in? %>
      			<%= render("shared/signed_in_header") %>
			<%else%>
        		<%= render("shared/signed_out_header") %>
      		<%end%>
        
          
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

